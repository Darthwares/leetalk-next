/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'lh3.googleusercontent.com',
      'source.unsplash.com',
      'images.unsplash.com',
    ],
  },
};

export default nextConfig;
